<!DOCTYPE html>
    <html>
        <head>
            <link type="text/css" rel="stylesheet"  href="user_login.css">
        </head>
        <body>
            <div class="slogan-container">
                <h2>EAT. LEARN. CODE.</h2>
            </div>
            <div>
                <% if flash[:errors] %>
                    <h1>OOPS</h1>
                    <% flash[:errors].each do |err| %>
                        <%= err %><br>
                    <% end %>
                <% end %>
                <%= form_tag login_path do |f| %>
                    <%= label_tag :username%>:  <br>
                    <%= text_field_tag :username %><br>
                    <%= label_tag "Password:" %><br>
                    <%= password_field_tag :password %><br>
                    <%= submit_tag "Log in"%>
                <% end %>
            </div>

            <h2>Don't Have an Account With Chipotle? Sign Up!</h2>
            <%= link_to "Sign Up Now!", new_user_path %>
        </body>
    </html>